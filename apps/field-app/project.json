{
  "name": "field-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/field-app/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project field-app --web",
  "targets": {
    "generate-env": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/generate-field-app-env.js"
      }
    }
  }
}
